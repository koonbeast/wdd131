semantic - name describes the meaning, non semantic is basic without innate purpose like div and span, semantic tags don't need aria labels because semantic tags already are innately descriptive

element vs root element

there should be no css blocks of code after media queries

aria labels - includes meaning of popup windows

placeholder - default shaddow text within an input field

push = append

parenthesis are necessary for functions

ul = bullet list
ol = numbered list?